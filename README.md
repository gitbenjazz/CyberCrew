git clone https://github.com/gitbenjazz/CyberCrew.git
cd CyberCrew
python3.11 -m venv .venv
source .venv/bin/activate
  1  pip install -r requirements.txt
ou alors plus specifiquement
  2 pip install -r requirements.lock.txt


  
